---
layout: null
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ page.url | absolute_url | xml_escape }}</id>
  <link href="{{ page.url | absolute_url | xml_escape }}" rel="self" type="application/atom+xml"/>
  <title>{{ site.title | default: site.name | smartify | xml_escape }}</title>
  {% if site.description %}
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  {% endif %}
  <link href="{{ '/' | absolute_url | xml_escape }}" rel="alternate" type="text/html"/>
  {% if site.social.name %}
  <author>
    <name>{{ site.social.name | xml_escape }}</name>
    {% if site.social.email %}
    <email>{{ site.social.email | xml_escape }}</email>
    {% endif %}
    {% if site.social.links[0] %}
    <uri>{{ site.social.links[0] | xml_escape }}</uri>
    {% endif %}
  </author>
  {% endif %}
  {% assign posts = site.posts | where_exp: 'item', 'item.hidden != true' | sort: 'date' | reverse %}
  {% assign posts_limit = 10 %}
  {% for post in posts limit: posts_limit %}
  <entry>
    <title>{{ post.title | smartify | strip_html | normalize_whitespace | xml_escape }}</title>
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</updated>
    <id>{{ post.id | absolute_url | xml_escape }}</id>
    <link href="{{ post.url | absolute_url | xml_escape }}" rel="alternate" type="text/html" title="{{ post.title | smartify | strip_html | normalize_whitespace | xml_escape }}"/>
    {% if site.social.name %}
    <author>
      <name>{{ site.social.name | xml_escape }}</name>
    </author>
    {% endif %}
    <content type="html">{{ post.content | strip_html | normalize_whitespace | xml_escape }}</content>
  </entry>
  {% endfor %}
</feed>
